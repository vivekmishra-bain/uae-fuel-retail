name: Build and Deploy to Azure
on:
  push:
    branches: [main]
  workflow_dispatch:
env:
  REGISTRY: acrdubaifueup617.azurecr.io
  IMAGE_NAME: wal-dubai-fu-eu-p-553-production
  WEBAPP_NAME: wal-dubai-fu-eu-p-553-production
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Log in to Azure Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.ACR_USERNAME_DUBAI }}
          password: ${{ secrets.ACR_PASSWORD_DUBAI }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_DUBAI }}
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
